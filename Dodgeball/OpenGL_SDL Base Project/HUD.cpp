#include "HUD.h"

HUD::HUD() {

}

HUD::~HUD() {

}

void HUD::Render() {

	//glDisable(GL_TEXTURE_2D);
	//glDisable(GL_LIGHTING);
	//glDisable(GL_LIGHT0);

	//glColor3f(1.0, 1.0, 1.0);
	//glMatrixMode(GL_MODELVIEW);
	//glPushMatrix();
	//glLoadIdentity();
	//glMatrixMode(GL_PROJECTION);
	//glPushMatrix();
	//glLoadIdentity();
	//gluOrtho2D(0, 100, 0, 100);
	//OutputLine(45, 95, "Robot Arm");
	//OutputLine(5, 95, "Camera Controls:");
	//OutputLine(5, 92, "W - move forward");
	//OutputLine(5, 89, "S - move backward");
	//OutputLine(5, 86, "A - move left");
	//OutputLine(5, 83, "D - move right");
	//OutputLine(5, 80, "Up arrow - look up");
	//OutputLine(5, 77, "Down arrow - look down");
	//OutputLine(5, 74, "Left arrow - look left");
	//OutputLine(5, 71, "Right arrow - look right");
	//OutputLine(5, 5, "Left click - start base rotation");
	//OutputLine(5, 2, "Right click - stop base rotation");
	//OutputLine(30, 5, "1 - Upper Arm bone up");
	//OutputLine(30, 2, "2 - Upper Arm bone down");
	//OutputLine(50, 5, "3 - Lower Arm bone up");
	//OutputLine(50, 2, "4 - Lower Arm bone down");
	//OutputLine(75, 5, "5 - Hand bone up");
	//OutputLine(75, 2, "6 - Hand bone down");

	//glMatrixMode(GL_PROJECTION);
	//glPopMatrix();
	//glMatrixMode(GL_MODELVIEW);
	//glPopMatrix();

	//glEnable(GL_LIGHTING);
	//glEnable(GL_LIGHT0);
}

void HUD::OutputLine(float x, float y, string text) {

	//glRasterPos2f(x, y);  // where to start drawing
	//for (int i = 0; i < text.size(); i++) {
	//	glutBitmapCharacter(GLUT_BITMAP_HELVETICA_12, text[i]);
	
}